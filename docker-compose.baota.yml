services:
  app:
    build:
      # 从项目目录执行 docker compose 时，当前目录就是源码目录
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3000"
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
