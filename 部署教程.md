# NovaCanvas 公司内部部署教程

## 适用场景

将 NovaCanvas 部署到公司自己的 Linux 服务器，使用公司邮箱和密码登录，不依赖 GPT 账号。

## 宝塔面板快速部署（推荐）

如果服务器使用宝塔面板，不需要在命令行手动安装复杂环境。建议通过宝塔的 **Docker 管理器 / Compose 项目** 部署。

### 推荐：GitHub 私有仓库 + 宝塔 Docker

此方式适用于已经在 GitHub 建立了 NovaCanvas **私有仓库** 的情况。不要把 `.env`、API 密钥或数据库密码上传到 GitHub。

#### 一次性准备 GitHub 仓库

1. 在 GitHub 新建私有仓库，例如 `your-company/NovaCanvas`，不要初始化 README 或 `.gitignore`。
2. 在开发电脑的项目目录提交代码并推送到该仓库。`.gitignore` 已排除 `.env`、构建文件和部署压缩包。
3. 服务器端需要使用只读 Deploy key 拉取仓库：在宝塔终端执行：

```bash
ssh-keygen -t ed25519 -C "github-deploy-key" -f ~/.ssh/github_deploy_key -N ""
cat ~/.ssh/github_deploy_key.pub
```

4. 将输出的 `ssh-ed25519 ...` 整行复制到 GitHub 仓库：**Settings → Deploy keys → Add deploy key**。名称填 `baota-server`，不要勾选 **Allow write access**。

#### 首次部署到宝塔

以下示例使用域名 `design.your-company.com`；请替换为你的实际域名，并确保该域名未被服务器上的其他站点使用。

1. 在宝塔 **网站** 新建站点，域名填你的新域名，根目录会自动创建为 `/www/wwwroot/你的域名`。PHP、数据库和 FTP 均选择“不创建”。
2. 在宝塔终端执行（将仓库地址和域名替换成实际值）：

```bash
cd /www/wwwroot/design.your-company.com
git config --global --add safe.directory /www/wwwroot/design.your-company.com
git init
git remote add origin git@github.com:your-company/NovaCanvas.git
GIT_SSH_COMMAND="ssh -i ~/.ssh/github_deploy_key -o IdentitiesOnly=yes" git fetch origin main
git checkout -B main origin/main
cp .env.example .env
```

3. 在宝塔文件管理中编辑 `.env`，填写数据库密码、管理员邮箱和密码、实际 HTTPS 域名，以及至少一个作图 API 密钥。
4. 回到宝塔终端执行：

```bash
cd /www/wwwroot/design.your-company.com
docker compose up -d --build
docker compose ps
```

5. 确认 `app` 和 `db` 都是 `running` 后，在该宝塔站点添加反向代理：`http://127.0.0.1:3001`，申请 SSL 并开启强制 HTTPS。

#### 后续更新

服务器不需要重新上传压缩包。每次发布新版本后，宝塔终端在项目目录执行：

```bash
GIT_SSH_COMMAND="ssh -i ~/.ssh/github_deploy_key -o IdentitiesOnly=yes" git fetch origin main
git checkout -B main origin/main
docker compose up -d --build
```

`.env` 是服务器本地文件，不会被 Git 覆盖。

### 方式 A：应用和数据库一起部署（最省事）

1. 在宝塔的文件管理中，将整个项目上传到例如 `/www/wwwroot/novacanvas`。
2. 复制 `.env.example` 为 `.env`，填写管理员账号、模型 API 密钥和强数据库密码。
3. 打开宝塔 **Docker → Compose**，新增项目，选择项目目录和 `docker-compose.yml`。
4. 点击构建并启动。宝塔会同时启动网站与 PostgreSQL 数据库。
5. 在宝塔 **网站** 中新增站点并反向代理到 `127.0.0.1:3001`，申请 SSL 证书。

这是最推荐的方式：网站和数据库由同一套 Compose 管理，后续更新、备份和迁移最简单。

### 方式 B：使用宝塔单独创建的 PostgreSQL 数据库

如果你已经在宝塔安装了 PostgreSQL，也可以只通过 Docker 运行网站：

1. 在宝塔 PostgreSQL 管理中创建数据库 `novacanvas`、用户 `novacanvas` 和强密码。
2. 复制 `.env.example` 为 `.env`，将 `DATABASE_URL` 改为你的数据库连接。例如数据库也在同一台宝塔服务器：

```text
DATABASE_URL=postgres://novacanvas:你的数据库密码@host.docker.internal:5432/novacanvas
```

3. 在宝塔 **Docker → Compose** 中创建项目，选择 `docker-compose.baota.yml` 启动。
4. 同样在宝塔 **网站** 中配置反向代理和 SSL。

注意：当前自托管版本使用 PostgreSQL；若你宝塔只安装了 MySQL，需要先安装 PostgreSQL，或让我把数据库层改为 MySQL。

### 方式 C：宝塔 Node 项目（不使用 Docker，最适合你当前服务器）

如果 Docker 编排反复报错，直接使用宝塔的 **网站 → Node 项目**：

1. 上传并解压项目到 `/www/wwwroot/novacanvas`。
2. 在宝塔 Node 项目中新增项目，项目目录选择 `/www/wwwroot/novacanvas`。
3. 启动文件填写 `server.js`，端口填写 `3001`，Node 版本选择 22。
4. 在项目目录执行一次依赖安装和构建：`npm ci`、`npm run build`。
5. 在宝塔网站中把域名反向代理到 `http://127.0.0.1:3001`，然后申请 SSL。

这种方式不需要 Compose；请为 NovaCanvas 选择一个未被其他服务占用的端口。

## 准备条件

- 一台 Ubuntu 22.04/24.04 服务器，建议 2 核 4GB 内存、40GB 磁盘起步。
- 一个域名，例如 `design.your-company.com`，并将域名 A 记录指向服务器公网 IP。
- 服务器已安装 Docker 与 Docker Compose。
- OpenAI 或 Gemini 的作图 API 密钥。至少配置其中一种。

## 通用部署步骤

### 第一步：上传项目

将整个项目上传到服务器，例如 `/opt/novacanvas`。

### 第二步：创建生产环境变量

在项目目录复制 `.env.example` 为 `.env`，并修改以下内容：

- `POSTGRES_PASSWORD`：改为长随机密码。
- `DATABASE_URL`：其中的数据库密码必须与 `POSTGRES_PASSWORD` 完全一致。
- `APP_URL`：改为你的实际 HTTPS 域名。
- `BOOTSTRAP_ADMIN_EMAIL`：首个管理员的公司邮箱。
- `BOOTSTRAP_ADMIN_PASSWORD`：首个管理员的强密码，至少 16 位。
- `OPENAI_API_KEY` 或 `GEMINI_API_KEY`：至少填写一项。

不要把 `.env` 提交到 Git 或发给普通员工。

### 第三步：启动

在项目目录运行：

```bash
docker compose up -d --build
```

首次启动会自动创建 PostgreSQL 数据库和账号数据表。之后访问域名的 `/login`，用 `.env` 中的首个管理员账号登录。NovaCanvas 使用本机 `3001` 端口，避免与现有项目冲突。

### 第四步：配置 HTTPS 和域名

生产环境不要直接暴露 3001 端口。使用 Nginx 或 Caddy 做反向代理，并为域名申请 HTTPS 证书。

Nginx 核心配置示例：

```nginx
server {
  listen 80;
  server_name design.your-company.com;
  location / {
    proxy_pass http://127.0.0.1:3001;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
```

启用 HTTPS 后，将 `.env` 的 `APP_URL` 更新为 `https://design.your-company.com`，再重新执行启动命令。

### 第五步：完成管理员初始化

管理员首次登录成功后，删除 `.env` 中以下三项的值，然后重新启动服务：

- `BOOTSTRAP_ADMIN_EMAIL`
- `BOOTSTRAP_ADMIN_PASSWORD`
- `BOOTSTRAP_ADMIN_NAME`

这样初始化密码不会长期保留在服务器配置中。

## 日常运维

- 查看日志：`docker compose logs -f app`
- 更新版本：备份数据库后，上传新代码并运行 `docker compose up -d --build`
- 备份数据库：使用 `pg_dump` 导出 PostgreSQL，至少每日备份一次并保存到服务器外部。
- 模型密钥泄露或员工离职时，应立即在服务商平台轮换 API 密钥与账号密码。

## 上线前检查

- 域名 HTTPS 已启用。
- `.env` 不在 Git 仓库中。
- 数据库密码、管理员密码均为强随机密码。
- 已配置至少一个可用作图 API 密钥。
- 已建立每日数据库备份。
